services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: walletguard
      POSTGRES_PASSWORD: walletguard
      POSTGRES_DB: walletguard
    ports:
      - "5432:5432"
    volumes:
      - walletguard_pg:/var/lib/postgresql/data

volumes:
  walletguard_pg:
